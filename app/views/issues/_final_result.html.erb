<div class="row">
  <div data-aos="zoom-in" class="col-xs-12 offset-md-1 col-md-10 transparent-container solution-container mb-5 mt-5">
    <h2 class="text-center text-white border-bottom"><div class="asset-title">Results</div></h2>
    <div class="container-overflow">
      <% issue.solutions.each do |solution| %>
        <% if solution.bobby == " Excellent" %>
          <h2 class= "vote-content"><%=  solution.content  %></h2>
          <h3 class= "vote-content font-italic seperate-content mb-3">Indice | <span class = "text-white font-italic text-success"><%= solution.bobby  %><span></h3>
        <% end %>
      <% end %>
      <% issue.solutions.each do |solution| %>
          <% if solution.bobby == " Très bien" %>
          <h2 class= "vote-content"><%=  solution.content %></h2>
          <h3 class= "vote-content font-italic seperate-content mb-3">Indice | <span class = "text-white font-italic text-primary"><%= solution.bobby  %></span></h3>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
